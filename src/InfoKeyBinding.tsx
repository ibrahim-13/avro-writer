import React from 'react';

export function InfoKeyBinding(): JSX.Element {
  return (
    <div className="info-keybinding">
      (CTRL + . to Toggle Language, arrow keys to traverse suggestions, enter to apply suggestion)
    </div>
  );
}